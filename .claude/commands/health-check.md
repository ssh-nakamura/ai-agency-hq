---
name: health-check
description: 組織の健全性チェック。validate-docs + 放置アクション検出 + 構造整合性を一括実行する。
---

# 組織ヘルスチェック

## 引数
- $ARGUMENTS: なし（常にフルチェック）

## 実行手順

### 1. 構造検証（自動）

```bash
cd /Users/soshunakamura/ai_prodcut/ai-agency-hq && python3 -m pytest tests/ -v --tb=short 2>&1 | tail -20
```

テスト結果を報告:
- 全件パス → "✅ 構造検証: {N}テスト通過"
- 失敗あり → "❌ 構造検証: {N}件失敗" + 失敗内容

### 2. ドキュメント検証（自動）

```bash
python3 tools/validate-docs.py 2>&1
```

エラーがあれば報告。

### 3. 放置アクション検出（自動）

```bash
python3 -c "
import sys; sys.path.insert(0, '.')
from tools.core.status_parser import StatusParser
sp = StatusParser()
stale = sp.get_stale_actions()
if stale:
    print('⚠️ 放置アクション:')
    for a in stale:
        print(f'  {a[\"id\"]}: {a[\"action\"]} ({a[\"section\"]})')
else:
    print('✅ 放置アクションなし')
pending = sp.get_pending_approvals()
if pending:
    print(f'📋 株主承認待ち: {len(pending)}件')
    for p in pending:
        print(f'  {p[\"id\"]}: {p[\"action\"]}')
"
```

### 4. ファイル整合性チェック

以下を確認:
- 全エージェント定義（`.claude/agents/*.md`）に対応するMEMORY.mdが存在するか
- `docs/plan.md` と `docs/status.md` のフェーズ表記が一致するか
- `content/logs/` に直近7日以内のログがあるか（活動確認）
- `reports/index.html` が最新か

### 5. レポート

```
## 組織ヘルスチェック結果 - YYYY-MM-DD

| チェック項目 | 結果 |
|-------------|------|
| 構造テスト | ✅ {N}件通過 / ❌ {N}件失敗 |
| ドキュメント整合性 | ✅ / ❌ {エラー数} |
| 放置アクション | ✅ なし / ⚠️ {N}件 |
| 株主承認待ち | {N}件 |
| エージェント定義 | ✅ / ❌ |
| フェーズ一致 | ✅ / ❌ |
| 直近ログ | ✅ / ⚠️ {最終日} |

### 要対応
- {問題があれば列挙}
```

## 実施頻度
- **毎セッション開始時**に `/startup` と合わせて実行推奨
- 問題がなければ30秒で完了
